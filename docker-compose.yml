version: '2'
services:
  api:
    build: ./api-server
    command: npm start
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=develop
  app:
      build: .
      ports:
        - "80:80"
      links:
        - "api"
      # NOTE: this must match the copy command in Dockerfile
      # should override the source to make dev easier
      # volumes:
      #   - ./dist:/usr/share/nginx/html

